## context("S4")

## testthat::test_that("methods package is attach on ns-load", {
##   res <- base::system2('Rscript -e "dump <- dat::mutar;search()"', intern = TRUE)
##   testthat::expect_true(any(grepl("package:methods", res)))
## })
